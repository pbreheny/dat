[project]
name = "dat"
version = "1.14.2"
dependencies = [
    "boto3",
    "xxhash",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
dat = "dat:dat"

[project.optional-dependencies]
test = [
    "pytest",
    "moto[s3]",
]

