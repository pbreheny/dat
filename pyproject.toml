[project]
name = "dat"
version = "1.12.1"
dependencies = [
    "boto3",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
dat = "dat:dat"

[project.optional-dependencies]
test = [
    "pytest",
    "moto[s3]",
]

